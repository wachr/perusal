import { h } from "preact";

import { fireEvent, render, screen, waitFor } from "@testing-library/preact";

import GistStorage from "./GistStorage";

describe(GistStorage.name, () => {
  it.todo("can import from a gist");
  it.todo("can export to a gist");
  it.todo("can get a list of candidate gist ids");
});
